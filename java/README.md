# Java Prometheus example
This is a simple Java example app that shows how to use different stats in prometheus.

# TODO:
* Create Docker build/publish
* Add more histogram/counter/gauge examples
* Add example serving a prometheus endpoint w/o jetty/servlets.

